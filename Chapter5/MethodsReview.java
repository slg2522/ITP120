package allFiles;

public class MethodsReview
{
	//the same name can be used for local variables in different methods bc the methods cannot see each other's local variables
	
	//local variable lifetime: local variables only exist while the method is executing. When the method ends, local variables and parameters can no longer be held in the computer's memory.
	
	//methods can return/send values back to statements that called them.
	
	//strings can also be returned with methods
}
